# Wiki Schema & Operating Rules

이 문서는 위키의 구조, 문서 규격, 수록 기준, 편집 워크플로를 정의합니다. **AI 에이전트는 이 위키를 읽거나 수정하기 전에 반드시 이 문서를 따라야 합니다.** (Karpathy LLM Wiki의 schema layer에 해당)

## 1. 디렉터리 구조

```
<domain>/               # bluearchive | anothereden | endfield | lolpro
├── index.md            # 이 도메인의 전체 문서 목록 + 한 줄 요약 (탐색 진입점)
├── log.md              # append-only 변경 기록
├── glossary.md         # 고유명사 ko/en/ja 공식 표기 매핑 (i18n의 유일한 관리 지점)
├── guides/             # 시스템·컨텐츠 일반 가이드
├── raids/              # (도메인별 필요에 따라) 보스/전투 단위 문서
└── notes/              # 단일 주제 노트 (평가, 메타 분석, Q&A에서 파생된 지식)
```

### 언어 정책 (i18n)

- **본문은 한국어 단일 원본만 유지합니다.** 번역본 미러를 만들지 않습니다 — 이 위키의 1차 독자는 LLM이며, 답변 언어는 질의 시점에 LLM이 처리합니다. 번역 미러는 수정마다 관리 부담을 배수로 늘리고, 갱신이 누락된 번역본은 모순된 진술(drift)의 원천이 됩니다.
- **고유명사는 즉석 번역 금지.** 컨텐츠명·기믹명·커뮤니티 용어 등은 서버별 공식/통용 표기가 따로 있으므로, 다른 언어로 답할 때 반드시 `glossary.md`의 매핑을 따릅니다. 문서에 새 고유명사가 등장하면 glossary에도 등록합니다.
- **표기 매핑의 정본은 `glossary.md`입니다.** 문서 본문에 표기를 병기하는 것(예: 컨텐츠 가이드의 한/일 표)은 가독성을 위해 허용되지만 사본으로 간주합니다. 문서와 glossary가 충돌하면 glossary가 우선이며, 문서 안에만 있고 glossary에 없는 표기는 Lint에서 등록 대상으로 검출합니다.
- 학생/캐릭터 이름처럼 DB에 번역이 있는 정형 매핑은 MCP 도구가 담당합니다. glossary에는 MCP가 제공하지 못하는 것만 넣습니다.
- 특정 문서의 전문 번역이 꼭 필요해지면 해당 문서만 예외적으로 만들되, frontmatter에 원본 파일과 기준 커밋을 명시해 drift를 추적 가능하게 합니다.

### 파일 네이밍 컨벤션

파일명은 grep 탐색의 검색 인덱스이자 상호 링크·도구 매핑이 참조하는 **안정적 ID**입니다. 다음 규칙을 따릅니다.

1. **로마자 영문 슬러그, 소문자 + 언더스코어**: `binah.md`, `shiro_kuro.md`. 파일명에 한글·공백·대문자를 쓰지 않습니다. (인코딩·URL·도구 호환성)
2. **파일명 = 엔티티 슬러그**: 타입은 폴더가 나타내므로 파일명에 반복하지 않습니다 (`binah_raid_guide.md` ❌). 날짜·번호 접두어도 쓰지 않습니다 — 날짜는 frontmatter와 히스토리 섹션의 몫입니다.
3. **rename은 신중하게**: 표기·제목이 바뀌면 frontmatter의 `title`만 수정하고 파일명은 유지합니다. rename이 꼭 필요하면 참조 링크 전체 수정 + `log.md` 기록을 동반해야 합니다.
4. **별칭은 `aliases` frontmatter로**: 여러 이름으로 불리는 엔티티는 파일을 하나만 두고 별칭을 `aliases`에 적습니다. 파일을 이름별로 중복 생성하지 않습니다.
5. **notes/ 파일명은 구체적인 주제 슬러그**: 예: `notes/geburah_special_dealer_meta.md`. 세션 ID·날짜를 파일명에 넣지 않습니다.

### 재구성 (폴더 이동·문서 병합)

하위 폴더 구조는 도메인마다 달라도 되는 유연한 분류입니다. 시스템이 고정 계약으로 의존하는 것은 `index.md`와 `log.md`뿐이며, 외부 도구는 문서를 "도메인 + 파일 슬러그"로 참조합니다(전체 경로는 계약이 아님). 따라서 폴더 이동·병합은 예외 상황이 아니라 정상 운영(Lint의 일부)이고, 아래 절차만 지키면 됩니다.

- **이동**: `git mv`로 옮기고(히스토리 보존) → 옛 경로를 grep해서 들어오는 링크를 모두 수정 → `index.md` 갱신 → `log.md` 기록. 파일명(슬러그)은 유지합니다.
- **병합**: 내용을 남는 문서로 통합 → 사라지는 문서의 슬러그를 남는 문서의 `aliases`에 추가(옛 이름 검색 대응) → 사라진 문서를 가리키던 링크 수정 → `index.md` 갱신 → `log.md` 기록.
- 이동과 파일명 변경을 한 번에 하지 않습니다. (변경 추적이 어려워짐)

## 2. 문서 규격 (frontmatter)

모든 문서는 YAML frontmatter로 시작합니다.

```yaml
---
title: 비나 (Binah)            # 사람이 읽는 제목
type: guide                    # hub | guide | raid-guide | note | history
status: seeded                 # seeded | ai-draft | human-verified
aliases: []                    # (선택) 별칭·통칭·다른 표기. 검색 힌트로 쓰임
sources:                       # 근거. URL, "user-feedback", "seed" 등
  - seed: ba-analyzer 내장 가이드에서 이전 (2026-07-06)
created: 2026-07-06
updated: 2026-07-06
---
```

- `status` 의미: `seeded` = 기존 검증된 자료에서 이전됨 / `ai-draft` = AI가 작성, 사람 검토 전 / `human-verified` = 사람이 확인함.
- AI가 문서를 수정하면 `updated`를 갱신하고, 새 근거를 `sources`에 추가합니다.

## 3. 수록 기준 — 무엇을 위키에 넣는가

새 정보가 왔을 때 아래 순서로 판정합니다.

1. **MCP로 계산/조회 가능한 정형 데이터인가?** → 넣지 않는다. (예: 스킬 계수, 파티 통계)
2. **웹 검색 상위 결과나 공식 소스에서 바로 나오는가?** → 넣지 않는다. 검색에 맡긴다. (예: 이벤트 일정, 공지)
3. **시간이 지나면 답이 달라지는 평가·메타·티어인가?** → **넣는다.** 단, 덮어쓰지 말고 히스토리로 누적한다. (아래 5절)
4. **여러 소스를 종합해야만 나오는 결론인가?** (여론 정리, 논쟁 정리) → 넣는다.
5. **AI가 실제로 틀렸던 질문인가?** → **최우선으로 넣는다.** 피드백으로 확인된 질문-정답 쌍이 가장 가치 있는 콘텐츠다.
6. **우리만 가진 고유 데이터에 대한 해설인가?** (영상 분석 해석, 총력전 메타 변화) → 넣는다.

## 4. 새 페이지 vs 기존 페이지 수정

- **새 페이지**: 다른 문서에서 링크할 만한 독립 엔티티일 때. (예: 새 보스, 새 컨텐츠, 특정 논쟁 주제)
- **기존 페이지 수정**: 기존 엔티티의 속성 추가·갱신일 때.
- 새 페이지를 만들면 반드시 해당 도메인 `index.md`에 한 줄 요약과 함께 등록하고, 관련 문서에 상호 링크를 추가합니다.

## 5. 히스토리 규칙

평가·메타 정보는 **덮어쓰지 않습니다.** 문서 하단에 `## 히스토리` 섹션을 두고 시점과 함께 누적합니다.

```markdown
## 히스토리

- 2026-07: 신규 학생 XX 등장으로 기존 1티어 조합 대체됨 (근거: ...)
- 2026-03: 밸런스 패치로 평가 상승 (근거: ...)
```

"과거에는 사실이었던 진술"을 보존하는 것이 이 위키가 RAG·웹 검색과 차별화되는 지점입니다.

## 6. 편집 워크플로

1. **읽기**: `index.md` → 필요 시 키워드 검색 → 문서 열람 순서로 탐색합니다.
2. **쓰기 (AI)**: `main` 직접 push 금지. 브랜치를 만들어 **PR로 제출**합니다. PR 본문에 근거(출처 URL, 피드백 세션 요약, 판정 근거)를 반드시 포함합니다.
3. **쓰기 (사람)**: 직접 커밋 가능하나 PR을 권장합니다.
4. **모든 변경**은 도메인 `log.md` 최상단에 한 줄 append:

```markdown
- 2026-07-06 | ai | raids/binah.md 수정 | 안정성 디버프 수치 정정 | 근거: user-feedback session, 공식 패치노트 URL
```

형식: `- 날짜 | 주체(ai/human) | 대상 파일 | 변경 요약 | 근거`

5. **Lint (주기 점검)**: 모순되는 진술, 낡은 주장(히스토리 미반영), index 미등록 문서, 깨진 링크를 점검하고 발견 사항을 PR로 제안합니다.

## 7. 금지 사항

- 근거 없는 수치·평가 추가 금지. 확신이 없으면 문서에 넣지 말고 PR 본문에 질문으로 남깁니다.
- 사용자 피드백을 검증 없이 그대로 반영 금지. 웹 검색 등으로 교차 확인 후 반영하고, 확인 불가 시 `status: ai-draft`로 표시합니다.
- 다른 도메인 폴더의 문서를 임의로 수정 금지 (도메인 간 링크는 허용).
